# Грум-бриф: медиа R2 + Cloudflare Stream (из драфта task 5)

**Роль:** ты — грумер задачи. Кода НЕ пишешь. Результат — готовая к работе задача
`docs/kanban/todo/task_media_stream_5.md` уровня детализации
`docs/kanban/todo/task_real_auth_14.md` (эталон структуры: Контекст → Продуктовые решения →
Scope → Acceptance criteria → Out of scope → References) плюс отдельный **чек-лист ручных
действий в консоли Cloudflare** для Димы и список открытых вопросов с рекомендациями.

**Исходник:** `docs/tbd/task_media_stream_5.md` — сырой драфт. Направление верное
(R2 через `@payloadcms/storage-s3` для Media, Stream с `requireSignedURLs`, серверная
генерация playback-токенов, выдача PDF через приложение), детализация — нет.

## Что уже есть в коде (проверь и отрази в Контексте задачи)

- `src/collections/Media.ts` — коллекция загрузок; сейчас хранение локальное. Выясни,
  как она используется (обложки курсов? PDF уроков?) и что изменит переезд в R2.
- `src/collections/Lessons.ts` — поля `streamVideoId`, `pdf`, `isFreePreview`; field-level
  access на защищённое содержимое уже работает (`canReadLessonContent`) и покрыт e2e.
  Значит, «кому отдавать токен» уже решаемо через `hasPaidAccess` — задача про «как отдавать».
- `src/components/player/CoursePlayerClient.tsx` — текущий плеер: посмотри, что он делает
  со `streamVideoId` сейчас и какой контракт ему нужен от сервера (подписанный URL/токен).
- `src/lib/access/hasPaidAccess.ts`, `src/lib/auth/` — авторизация и проверка покупки готовы.
- Локализация: по BUSINESS_RULES видео и PDF **раздельные на язык** (EN/RU). Проверь,
  как это сейчас смоделировано в Lessons (localized-поля?) — токены/выдача должны это уважать.

## Что сделать (по шагам)

1. **Прочитать:** драфт; `docs/ARCHITECTURE.md` (R2 для PDF/обложек, Stream для видео,
   signed URLs, топология site/admin-контейнеров); `docs/TZ.md` §9 (защита контента);
   ADR-3 в `docs/DECISIONS.md`; перечисленный выше код.
2. **Исследовать Cloudflare Stream** (developers.cloudflare.com/stream): механика
   `requireSignedURLs` — подписанные токены (JWT RS256 с signing key: генерация ключа,
   срок жизни токена, привязка к видео-uid), allowed origins, embed/iframe vs HLS/dash
   в собственном плеере; лимиты и цена (хранение/доставка за 1000 минут) — Диме для
   решения о подписке. Зафиксируй в задаче выбранный способ проигрывания (рекомендация:
   iframe-embed со signed token как самый короткий путь; свой плеер — отметить как альтернативу).
3. **Исследовать R2 + Payload:** `@payloadcms/storage-s3` — конфигурация под R2
   (endpoint, region auto, credentials), приватность бакета, как отдавать PDF **через
   приложение** (стрим из R2 после проверки доступа), а не прямыми ссылками. Это плагин ⇒
   правка `src/payload.config.ts`; проверь, нужна ли миграция (поля Media при смене адаптера).
4. **Зафиксировать продуктовые решения** (дефолты предложи сам, спорное — в вопросы):
   - TTL playback-токена и поведение при истечении посреди просмотра;
   - бесплатный тизер (`isFreePreview` + тизер на главной по BUSINESS_RULES): токен без
     аккаунта — как ограничиваем (только origin?);
   - миграция существующих локальных файлов Media в R2 (скрипт или руками — их пока мало);
   - что происходит в dev без CF-ключей (фолбэк на локальное хранение? мок токенов?) —
     важно для тестов и для работы Ани без секретов.
5. **Написать задачу** по эталонной структуре + отдельный блок «Ручные шаги (Дима, консоль CF)»:
   создать R2-бакет и API-токен, включить Stream, залить тестовое видео,
   `requireSignedURLs: true`, создать signing key, allowed origins = motophd.com/www,
   какие env-переменные положить в `.env` и в прод (`deploy/.env.prod.example` — только
   перечислить имена, файл не трогать).
   Acceptance criteria из драфта сохранить и дополнить: загрузка из админки → объект в R2 без
   публичной ссылки; видео играет только с валидным токеном (прямой URL — нет); PDF-роут — 403
   анониму/без покупки, отдаёт с покупкой; юнит на генерацию токена (моком ключа);
   тест на отказ; зелёные `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e`.
   Out of scope: вотермарк PDF, финальный UI плеера, привязка загрузки видео к урокам
   в админ-UX сверх существующего `streamVideoId`, CDN-тюнинг.
6. **Оценить объём**; больше ~3–4 дней — предложи разрез (например: R2+Media+PDF-роут /
   Stream+токены+плеер).

## Ограничения

- Только документы: изменяемые файлы — новая задача в `docs/kanban/todo/`, удаление старого
  драфта из `docs/tbd/` и правка его README-таблицы. Код, конфиги, `.env*` — не трогать.
- Git не трогать: не коммитить, не пушить, веток не создавать.
- **Координация с параллельным грумингом платежей (task 6):** обе задачи меняют схему БД.
  Зафиксируй в задаче правило: миграции создаются последовательно (кто первый в реализацию,
  тот первый; второй ребейзится). Media/Lessons — зона этой задачи; Purchases/промокоды —
  зона задачи 6. Пересечений полей быть не должно.
- Новые npm-зависимости (`@payloadcms/storage-s3`, jwt-библиотека если нужна) — не ставить,
  перечислить в задаче как «потребуют явного да».

**Финальный ответ в чат:** путь к готовой задаче + 5–10 строк: ключевые решения, чек-лист
ручных CF-шагов одной строкой, открытые вопросы Диме с рекомендациями, оценка объёма и разрез на PR.
