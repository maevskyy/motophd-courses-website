# Грум-бриф: платёжный каркас (из драфта task 6)

**Роль:** ты — грумер задачи. Кода НЕ пишешь. Результат — готовая к работе задача
`docs/kanban/todo/task_payments_skeleton_6.md` уровня детализации
`docs/kanban/done/*` / `docs/kanban/todo/task_real_auth_14.md` (это эталон структуры:
Контекст → Продуктовые решения → Scope → Acceptance criteria → Out of scope → References)
плюс короткий список открытых вопросов для Димы с рекомендуемыми ответами.

**Исходник:** `docs/tbd/task_payments_skeleton_6.md` — сырой драфт. Его скоуп в целом верен
(PaymentProvider-интерфейс, mock, скелет WayForPay, вебхук, идемпотентность, серверный прайсинг),
но контекст устарел — проверяй всё по коду.

## Известные устаревания драфта (проверь и исправь в задаче)

1. Драфт ссылается на task 14 (auth) как на будущую работу — **auth уже сделан**:
   `src/lib/auth/` (actions.ts с `payload.login`, currentUser.ts, redirect.ts). Задача должна
   опираться на фактический API этих модулей, а не на пересказ.
2. Коллекция `Purchases` уже существует (`src/collections/Purchases.ts`): user, course,
   tier `standard|feedback|feedback_upgrade`, amount, currency, provider
   `wayforpay|paypal|mock|manual`, status. Выясни, чего не хватает под задачу
   (минимум: `orderReference` + уникальный индекс, поле сырого колбэка/audit) —
   это изменение схемы ⇒ новая миграция в `src/migrations/`.
3. `hasPaidAccess` и field-level access уроков уже работают — гейт после `paid` Purchase
   откроется сам, отдельной работы не требует. Проверь по `src/lib/access/hasPaidAccess.ts`
   и `src/collections/Lessons.ts`.

## Что сделать (по шагам)

1. **Прочитать:** драфт; `docs/BUSINESS_RULES.md` (раздел «Деньги»: ~€29 / €129,
   feedback_upgrade = доплата разницы, промокоды в MVP, валюта EUR, возвратов нет);
   `docs/ARCHITECTURE.md` (инварианты: цена только на сервере, колбэки идемпотентны,
   дедуп по `orderReference`); `docs/TZ.md` §8; ADR в `docs/DECISIONS.md`;
   код: `src/collections/Purchases.ts`, `src/collections/Courses.ts` (price-поля),
   `src/lib/auth/`, `src/components/prototype/PricingBox/PricingBox.tsx` (текущая
   кнопка «купить» — точка входа чекаута).
2. **Исследовать WayForPay** (официальная дока wiki.wayforpay.com): формат запроса Purchase,
   состав строки для `merchantSignature` (HMAC-MD5, порядок полей, разделитель `;`),
   контракт serviceUrl-колбэка (какие поля, как подписан ответ мерчанта, ожидаемый
   `orderReference`-флоу), sandbox/тестовый мерчант, поддержка EUR и Apple/Google Pay.
   В задачу — точный формат подписи и **тест-вектор** (вход → ожидаемая подпись) для юнита.
3. **Зафиксировать продуктовые решения** (предложи дефолты, спорное — в вопросы Диме):
   - создание аккаунта после оплаты гостем: генерация пароля, письмо пока = лог
     (Resend — отдельная задача; это надо честно записать как известное ограничение:
     до Resend покупатель не получит доступ без ручного вмешательства);
   - покупка уже залогиненным пользователем — линковка на существующий аккаунт;
   - повторная покупка того же курса тем же юзером — отказ или no-op;
   - что видит пользователь после редиректа с оплаты (success/fail страницы) — хотя бы
     минимальные заглушки, т.к. флоу без них не тестируется e2e;
   - модель промокодов: где живут (новая коллекция ⇒ миграция), percent/fixed, лимиты, срок.
4. **Написать задачу** по эталонной структуре. Обязательно внутри:
   - Scope со списком конкретных файлов/модулей (`src/lib/payments/…`, роут колбэка,
     изменения `Purchases`, миграция, seed промокода для теста);
   - Acceptance criteria из драфта — сохранить и дополнить (e2e на mock-флоу гость→аккаунт→
     доступ; идемпотентный повторный колбэк; невалидная подпись = 400; юниты pricing и подписи);
   - зелёные `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e`;
   - Out of scope: боевые ключи (блокер — ФОП Влада), PayPal, полноценный UI чекаута
     с двумя чекбоксами (отдельная задача), реальные письма/Resend, rate-limit.
5. **Оценить** объём: если задача больше ~3–4 дней работы — предложи разрез на 2 PR
   (например: прайсинг+модель+mock-флоу / WayForPay-скелет+подпись+вебхук).

## Ограничения

- Только документы: единственный изменяемый файл — новая задача в `docs/kanban/todo/`
  (+ можно удалить старый драфт из `docs/tbd/` и поправить его README-таблицу).
- Git не трогать: не коммитить, не пушить, веток не создавать.
- **Координация с параллельным грумингом медиа (task 5):** обе задачи меняют схему БД.
  В задаче зафиксируй: миграции пишутся последовательно — кто первым уходит в реализацию,
  тот и создаёт миграцию первым, второй ребейзится. Purchases/промокоды — зона этой задачи;
  Media/Lessons — зона задачи 5. Пересечений полей быть не должно.
- Технологии не выбирать заново: стек и провайдеры зафиксированы в ARCHITECTURE/DECISIONS.
  Новые npm-зависимости — только перечислить в задаче как «потребуют явного да».

**Финальный ответ в чат:** путь к готовой задаче + 5–10 строк: ключевые решения,
открытые вопросы Диме (с твоими рекомендациями), оценка объёма и предложенный разрез на PR.
